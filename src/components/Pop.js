import React from 'react'
import { songs } from './SongsList'
export default function Pop(props) {
    // console.log(props.id);
  return (<></>
    // <>{songs.map((v)=> <div className="popup" id={v.id}> {v.song}</div> )}</>
  )
}


